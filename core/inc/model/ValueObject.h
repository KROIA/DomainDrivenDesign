#pragma once
/*#include "DDD_base.h"
#include "utilities/IID.h"

namespace DDD
{
	class ValueObject : public IID
	{
	public:
		ValueObject(ID id)
			: IID(id) {}
		virtual ~ValueObject() {}

		virtual bool operator==(const ValueObject& other) const = 0;
		virtual bool operator!=(const ValueObject& other) const = 0;

	private:

	};
}*/